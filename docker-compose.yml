version: "3.9"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      - OVERBUILD_CACHE_BACKEND=memory
      - OVERBUILD_DEBUG=true
    volumes:
      - ./src:/app/src

